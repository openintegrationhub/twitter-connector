/* eslint no-unused-expressions: "off" */

const { expect } = require('chai');

describe('Triggers - getUsers', () => {
  it('should test a thing', async () => {
    expect(true).to.be.true;
  });
});
